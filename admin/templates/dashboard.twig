{% extends "layout.twig" %}

{% block title %}Dashboard{% endblock %}

{% block heading %}Dashboard{% endblock %}

{% block content %}
    <div class="dashboard-stats">
        <div class="stat-card">
            <p class="heading">Bots</p>
            <p class="title">{{ stats.bots }}</p>
        </div>
        <div class="stat-card">
            <p class="heading">Documents</p>
            <p class="title">{{ stats.docs }}</p>
        </div>
        <div class="stat-card">
            <p class="heading">Embeddings</p>
            <p class="title">{{ stats.embeddings }}</p>
        </div>
        <div class="stat-card">
            <p class="heading">Conversations</p>
            <p class="title">{{ stats.conversations }}</p>
        </div>
        <div class="stat-card">
            <p class="heading">Configured Rooms</p>
            <p class="title">{{ stats.rooms }}</p>
        </div>
    </div>

    <div class="box">
        <h2 class="subtitle has-text-weight-semibold">Welcome to the Multi-Bot Admin Panel</h2>
        <div class="content">
            <p>Manage multiple AI chatbots, each with their own personality, knowledge base, and behavior. Create specialized bots for different teams, topics, or use cases.</p>
            
            <h3 class="title is-5 mt-5 mb-3">
                <span class="icon-text">
                    <span class="icon has-text-primary">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                        </svg>
                    </span>
                    <span>Getting Started</span>
                </span>
            </h3>
            
            <div class="columns">
                <div class="column">
                    <div class="notification is-info is-light">
                        <h4 class="title is-6">🤖 Create Your First Bot</h4>
                        <p class="mb-3">Start by creating a new AI bot with a unique mention name (like <code>@supportbot</code> or <code>@helpdesk</code>).</p>
                        <a href="{{ url_for('bot-create') }}" class="button is-info is-small">
                            <span class="icon">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <line x1="12" y1="5" x2="12" y2="19"/>
                                    <line x1="5" y1="12" x2="19" y2="12"/>
                                </svg>
                            </span>
                            <span>Create Bot</span>
                        </a>
                    </div>
                </div>
                <div class="column">
                    <div class="notification is-success is-light">
                        <h4 class="title is-6">📚 Build Knowledge Base</h4>
                        <p class="mb-3">Upload documents and training materials to give your bots specialized knowledge for different topics.</p>
                        <a href="{{ url_for('documents') }}" class="button is-success is-small">
                            <span class="icon">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                                    <polyline points="14,2 14,8 20,8"/>
                                </svg>
                            </span>
                            <span>Manage Documents</span>
                        </a>
                    </div>
                </div>
            </div>
            
            <h3 class="title is-5 mt-5 mb-3">
                <span class="icon-text">
                    <span class="icon has-text-primary">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/>
                        </svg>
                    </span>
                    <span>Multi-Bot Features</span>
                </span>
            </h3>
            
            <div class="content">
                <ul>
                    <li><strong>Unique Bot Mentions:</strong> Each bot responds to its own mention name (e.g., <code>@educai</code>, <code>@support</code>)</li>
                    <li><strong>Specialized Knowledge:</strong> Upload different documents for each bot's domain expertise</li>
                    <li><strong>Custom Personalities:</strong> Configure unique system prompts, models, and onboarding flows</li>
                    <li><strong>Room Assignment:</strong> The first bot mentioned in a chat claims that room</li>
                    <li><strong>Independent Settings:</strong> Each bot has its own RAG settings, AI models, and behavior</li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %} 